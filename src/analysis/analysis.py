""" This file contains the essential functions for data analysis. """

#import packages here

def compute_agg(data, agg_fn, agg_unit, out_path):
    """
    aggregate data using parameters in data_params.json
    output: aggregated data
    """
    agg = ...
    # output to out_path
    return agg
